# SUBSISTENCE — ответы v2 (19/19) и что сделано в alpha 1.0.0

Твои ответы: `КОРОТКО: 1б 2а 3в 4а 5в 6а 7а 8а 9в 10а 11а 12а 13а 14а 15а 16а 17в 18в 19б`

Ниже — что каждый ответ значит и **где это лежит в коде/моделях**. Если что-то не совпало
с ожиданием — правится в одну строку, файл указан рядом.

| № | Ответ | Что сделано | Где в проекте |
|---|-------|-------------|----------------|
| 1б | Апгрейд верстака на месте — **убрать**, только снос + постройка | Механика v0.2.0 WB1→WB2→WB3 вырезана. `TryUpgrade` больше не улучшает, а вежливо объясняет: «Апгрейда на месте нет: снеси киянкой и построй верстак N уровня». Снос киянкой по-прежнему возвращает ресурсы (10 минут, потом нет) | `Crafting/WorkbenchSystem.cs`, `Player/Deployables.cs` |
| 2а | Радиус верстака 4 м — как сейчас | Оставлено 4 м | `WorkbenchStation.radius = 4.0f` |
| 3в | Скорость крафта **×1 / ×2 / ×3** | Было ×1.0/×1.5/×2.0 — стало жёстче: T1 ×1, T2 ×2, T3 ×3. Скорость пишется в тост при подходе и в палитру крафта | `WorkbenchSystem.SpeedMult`, `Player/DeployPlacer.cs` |
| 4а | Чужим верстаком можно пользоваться всем (как в Rust) | Проверок владельца нет: подошёл — крафтишь. В тосте даже написано «(чужой тоже подходит)» | `Player/Deployables.cs` |
| 5в | 3 модели верстаков **и разные палитры по уровням** | 3 модели × 3 уровня = **9 вариантов**: L0 — тёплое дерево + жёлтая лампа; L37 — выгоревшее, серо-бирюзовый металл, холодный свет; L3 — тёмный промасленный металл, янтарная лампа. Выбор автоматический по зоне, где ставишь верстак | `tools/blender/models_workbench.py` (PALETTES), `World/ModelLibrary.cs` (`ForDeployableAt`, `LevelTag`) |
| 6а | Киянка как сейчас (ремонт ¼ цены, +40 % прочности) | Оставлено. Снос с возвратом 10 минут, потом только лом | `Building/BuildSystem.cs`, `BuildHammer` |
| 7а | Мягкая сторона ×2.5 — как сейчас | Оставлено | `BuildBlock.SoftSideMultiplier` |
| 8а | Лимит сноса 10 минут — как сейчас | Оставлено | `BuildBlock.DemolishRefundSeconds = 600f` |
| 9в | Добавить элементы стройки: **рампа-угол, перила, ставни** | Было 12 — стало **15**: горячие клавиши `[` `]` `\`. Перила/ставни дешевле (15 дерева), у перил HP 60 % от стены, у ставней 80 % | `Building/BuildSystem.cs`, `UI/BuildPaletteUI.cs` |
| 10а | Сделать модели **всем 6** «кубовым» элементам | Нарисованы: `BD_roof` (скат+конёк+стропила), `BD_ramp` (настил 3 м, подъём 1.5 м, бортики), `BD_pillar` (0.36×3 м, база+оголовок), `BD_foundation_tri`, `BD_floor_tri`, `BD_high_wall` (3×6 м, два пояса). Привязка по элементам — в `ForBuildPiece` | `tools/blender/models_buildpieces.py`, `World/ModelLibrary.cs` |
| 11а | Свои предметы уровней — **до 4 на уровень** | Стало по 4: L0 — светогриб, скотч, лампа-переноска, респиратор · L37 — маска, хлорка, ласты, кислородный баллон · L3 — перчатки, предохранитель, сапоги, изолированный ключ. Тег уровня строгий (вещи уровней не пересекаются), у каждого — своя Blender-модель `IT_*` | `Core/Items.cs`, `tools/blender/models_levelitems.py`, `World/LootSpawner.cs` |
| 12а | Атмосфера уровня — как есть | Туман/свет/ambient по уровням оставлены | `World/Atmosphere.cs` |
| 13а | Процедурный звук — как есть | Оставлено: 23 клипа считаются математикой, ноль .wav | `Audio/ProcAudio.cs` |
| 14а | Громкости фона — как сейчас | Оставлено. Настройка `AudioDirector` ползунками меню работает | `Audio/AudioDirector.cs`, `UI/OptionsUI` |
| 15а | Амплитуды анимации монстров — как сейчас | Оставлено (4 фазы × 8 монстров, процедурно) | `AI/MonsterMotion.cs` |
| 16а | **Музыки и радио нет** — только атмосфера | Проверено: музыкального слоя и радиостанций в проекте нет вообще — есть только звуки мира (гул лампы, капель, шаги, рык монстра). В шапке `AudioDirector` это закреплено комментарием, чтобы «музыка погони» не вернулась | `Audio/AudioDirector.cs` |
| 17в | exe — **оба варианта** | Есть `BUILD_WINDOWS.bat` (режим `play`, детект лицензии, окно 1280×720) и пункт меню Unity «Subsistence → 4/5. Собрать Windows». Оба описаны в `BUILD_WINDOWS.md` | `BUILD_WINDOWS.bat`, `Editor/ProjectBootstrap.cs` |
| 18в | Дальше — **сеть: тест на 2–4 игрока (Mirror)** | Каркас готов: `Net/` (Mirror v96), AOI 120 м, сервер-авторитет. Включение и тест — в `docs/MULTIPLAYER.md` (следующий шаг после альфы) | `Net/*.cs`, `Packages/manifest.json` |
| 19б | Mirror пока **офлайн** | Единственный игрок = хост, сеть не поднимается, но архитектура на месте (все вызовы идут через `NetworkBridge`) | `Net/NetworkBridge.cs` |

## Что стало с деревом исследований (блок 6 без ответа)

На блок 6 (3 вопроса про дерево) ответов не было — оставил ★-рекомендации, они же текущая
реализация: цена изучения = редкость × тир, гейт по верстаку, 23 дефолтных чертежа.
Подробности и таблица цен — в `docs/TECH_TREE.md`.

## Проверки

* `tools/check_cs.py` — 51 файл, 0 ошибок синтаксиса;
* `tools/check_cs_api.py` — 51 файл / 230 типов / 0 находок (проверки A–S: связи, CS0246,
  CS0102 дубли членов, CS0506 override, CS8773 структуры, CS0592 атрибуты, C#10+ синтаксис;
  P — член затеняет namespace, Q — метод чужого класса без префикса, R — имя не в этой области
  видимости, S — словарь без `.Value`, T — мертвая переменная). Эти пять добавлены после второй
  компиляции: все ошибки раундов 1–2 (CS0246/CS1061/CS0103/CS0219), линтер теперь ловит сам;
  проверка T сразу нашла три реальные мёртвые переменные в проекте — они убраны;
* `tools/self_test_lint.py` — самотест линтера на синтетическом проекте `tools/_selftest`,
  который повторяет все ошибки раундов: 8 находок из 8, посторонних нет;
* `tools/check_net_scale.py` — N1–N9: формат снапшота и его приёмник не разъезжаются,
  AOI+LOD на месте, лимиты онлайна и пороги приёмки трафика заданы;
* `docs/previews/_sheet_buildpieces.png` — 9 элементов стройки, `_sheet_levelitems.png` — 12 предметов,
  `_sheet_workbench_levels.png` — 9 вариантов верстаков.
